// Inventory simulator settings require the Inventory Simulator plugin.
// Minimal mode disables CSSharp plugins, so keep this empty.
echo "[custom] settings/inventory_simulator.cfg loaded (minimal)"
